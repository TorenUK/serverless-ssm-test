import Plugin from 'serverless/classes/Plugin';
import { Options, ServerlessWithError } from './types';
export default class ServerlessSsmPlugin implements Plugin {
    hooks: Plugin['hooks'];
    options: Options;
    serverless: ServerlessWithError;
    region: string;
    secretsFile: string;
    constructor(serverless: ServerlessWithError, options: Options);
    writeEnvironmentSecretToFile(): Promise<void>;
    getParameterFromSsm(name: string): Promise<Maybe<string>>;
    cleanupPackageSecrets(): Promise<void>;
    packageSecrets(): Promise<void>;
}
